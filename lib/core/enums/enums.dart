enum ErrorTypes { noConnectionInternet, timeout, serverError }

enum TransactionType { earn, redeem, transfer, adjustment }

enum PaymentMethod { cash, credit, mobile, voucher }

enum ReportType { sales, inventory, customerActivity, financial }
