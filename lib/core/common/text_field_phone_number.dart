import 'package:flutter/material.dart';

class TextFieldPhoneNumber extends StatelessWidget {
  const TextFieldPhoneNumber({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}